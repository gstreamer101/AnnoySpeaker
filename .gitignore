# Build output
plugin/builddir/
plugin/build/
**/*.o
**/*.dylib
**/*.so
**/*.a

# Meson/Ninja
.ninja_deps
.ninja_log
build.ninja
compile_commands.json

# macOS
.DS_Store
*.dSYM/

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Python (for upcoming GUI work)
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/

# Claude Code settings (personal — not shared)
.claude/settings.json
.claude/settings.local.json

# 개인 문서 (개발일지 + 사용가이드 등 — GitHub에 올리지 않음)
docs/

# 외부 참고 자산 (윈도우 Balabolka UI 스크린샷 등 — 저작권 회피)
balabolka_ui.img

# 도구 빌드 산출물 (clang/make로 만든 native binary)
tools/kb-tts-export/kb-tts-export

# PyInstaller 빌드 산출물 (.spec은 커밋해서 빌드 재현 가능하게)
gui/build/
gui/dist/

# 음성 산출물 (테스트로 만든 m4a 등)
*.m4a
*.wav
*.mp3
*.aiff
